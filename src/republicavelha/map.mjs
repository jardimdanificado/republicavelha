export function genMap(heightmap) 
{
    var blockMap = [];

    for (let x = 0; x < heightmap.length; x++) 
    {
		for (let y = 0; y < heightmap[0].length; y++) 
		{
			for (let z = 0; z < heightmap.maxHeight; z++) 
			{

			}
		}
    }
    return {block:blockMap};
  }